### Linear Time o(n)

```dart
void main() {
  void userNames(List<String> names) {
    for (final name in names) {
      print(name);
    }
  }

  userNames(['Nesta', 'Saint. Clever', 'Parchment']);
}
```